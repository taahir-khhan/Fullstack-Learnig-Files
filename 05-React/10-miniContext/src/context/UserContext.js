import React from "react";

// This creates a UserContext object, which can hold data that you want to share across components.
const UserContext = React.createContext();

export default UserContext;
