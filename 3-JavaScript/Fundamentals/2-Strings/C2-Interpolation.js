
// Concatenate (String interpolation)

const fname = "Tahir"
const lname = "Khan"

// old way of doing and if we have multiple string it reduces the code readability

console.log(fname + " " + lname);

// Bactics symbol , this way of writing and embeding string and variables are string interpolation (Currently used way)

console.log(`Fisrt name is ${fname.toLocaleUpperCase()} & Last name is ${lname}`);